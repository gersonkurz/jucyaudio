While loading the waveforms of a mix, show a progress dialog until all are loaded and cached, with a percentage.
vumeters don't work while playing a mix
If mix is playing, it doesn't stop when export starts - but you also cannot stop it even if you wanted to.
If not all mixes can be shown because a folder is unmounted, show them hidden - if selected, instead of showing data, show a special screen "not available because XYZ is not mounted"
