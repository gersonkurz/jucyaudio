# Agent Instructions

Primary project guidance lives in `CLAUDE.md`.

Operational notes:
- Use `docs/build.md` for build instructions (not `build.md` at repo root).
- Prefer validating changes with non-GUI checks when possible.
- GUI runtime behavior still requires manual human verification.
